# ==========================================================================================================
# Domain Filter Module
# ==========================================================================================================
# 용도: 쿼리 키워드 기반 도메인 필터링
# 키워드: "교육", "국방", "공공" 등 키워드가 쿼리에 있으면 해당 도메인 문서만 남김
# ==========================================================================================================

from data.loader import load_documents  # _classify_domain 로직 참조용 (필요 시 복사해서 사용)

def filter_by_domain(docs, query):
    """
    도메인 조건으로 문서 필터링
    
    Args:
        docs (List[Document]): 문서 리스트
        query (str): 사용자 질의
    
    Returns:
        List[Document]: 필터링된 문서 리스트
    """
    # [User Request] 쥬피터 노트북과 동일한 동작을 위해 도메인 필터링 비활성화
    # 원인: 필터링이 너무 강력해서 유효한 문서를 걸러내는 부작용 발생
    return docs
